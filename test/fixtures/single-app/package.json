{
  "name": "fixture-single-app",
  "private": true,
  "packageManager": "npm@10.0.0",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json",
    "test": "node --test"
  }
}
